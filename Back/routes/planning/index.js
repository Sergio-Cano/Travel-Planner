const router = require("express").Router();

module.exports = () => {
    router.get("/", );
    router.get("/:id", );
    router.get("/user/:user", );
    router.post("/create", );
    router.put("/:id", );
    router.delete("/:id", );
    router.get("/", );

    return router
}